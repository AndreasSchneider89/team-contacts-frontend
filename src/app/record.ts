// interface = Alle Felder müssen ausgefüllt werden
// Typ wird definiert
// Interface ist Beschreibung der Eigenschaften
export interface Record{
    id?: number;
    firstName: string;
    lastName: string;
    email: string;
}